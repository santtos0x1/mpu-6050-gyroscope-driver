#include "test_driver.h"
#include "pico_driver/error.h"
#include "test_registers.h"

pico_err_t test_setup_driver_hwr(void)
{
    uint8_t s_found = 0;

    for(int i = 0; i < 1000; i++)

    return PICO_OK_T
}